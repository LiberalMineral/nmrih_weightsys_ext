/**
 * @brief Set a client's maxcarry by index.
 * 
 * @param idx 			Index to Player(EntIndex)
 * @param weight		Weight
 * @return 				Succeeded or not
 */
native bool SetPlayerWeightByIdx(int idx,float weight);